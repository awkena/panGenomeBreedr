% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postgresql_variant_discovery.R
\name{pg_filter_by_af}
\alias{pg_filter_by_af}
\title{Filter extracted variants based on alternate allele frequency}
\usage{
pg_filter_by_af(
  gt,
  variant_id_col = "variant_id",
  chrom_col = "chrom",
  pos_col = "pos",
  min_af = 0,
  max_af = 1
)
}
\arguments{
\item{gt}{A data frame or matrix of variants x samples, typically
the output from \code{\link{pg_query_db}}.}

\item{variant_id_col}{Character. Column name for variant IDs. Default is 'variant_id'.}

\item{chrom_col}{Character. Optional column name for chromosome.}

\item{pos_col}{Character. Optional column name for genomic position.}

\item{min_af}{Numeric. Minimum alternate allele frequency threshold (0-1).}

\item{max_af}{Numeric. Maximum alternate allele frequency threshold (0-1).}
}
\value{
A data frame containing variant metadata and calculated frequencies
  for variants that passed the filter.
}
\description{
Calculates allele frequencies for a genotype matrix and filters variants
based on a user-defined range locally on your machine. Useful for removing
monomorphic or rare variants from pangenome queries.
}
\examples{
\dontrun{
library(panGenomeBreedr)

# Query region via the API and pipe into filter to remove rare variants (MAF < 0.05)
# Notice: No database connection needed!
filtered_vars <- pg_query_db(
  table_name = "genotypes",
  chrom = "Chr05",
  start = 75104537,
  end = 75106403
) |>
  pg_filter_by_af(min_af = 0.05, max_af = 0.95)
}

}
