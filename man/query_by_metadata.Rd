% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sqlite_variant_discovery.R
\name{query_by_metadata}
\alias{query_by_metadata}
\title{Query Genotypes Filtered by Sample Metadata Attributes}
\usage{
query_by_metadata(con, chrom, start = NULL, end = NULL, meta_col, meta_value)
}
\arguments{
\item{con}{A DBI connection object to the local database generated by \code{connect_local_db()}.}

\item{chrom}{Character. The target chromosome name (e.g., \code{"Chr05"}).}

\item{start}{Integer. Optional start coordinate for the target window region.}

\item{end}{Integer. Optional end coordinate for the target window region.}

\item{meta_col}{Character. The metadata schema column to filter samples by (e.g., \code{"countryorigin"}).}

\item{meta_value}{Character or Numeric. The explicit value attribute to match within \code{meta_col} (e.g., \code{"Mali"}).}
}
\value{
A wide-format data frame (variants x filtered samples) tracking core variant 
  properties alongside matching genotype calls.
}
\description{
Retrieves genotype calls for a specific genomic region, but restricts the wide-format 
column outputs to a subset of samples defined by matching metadata attributes (e.g., 
isolating variants only for specific countries of origin, breeding populations, or genetic clusters).
}
\examples{
\dontrun{
# Load the package
library(panGenomeBreedr)

# Connect to the database pipeline
con <- connect_local_db(folder_path = "~/Desktop/curated_sorghum_variant_resource")

# Get genotypes for a specific gene region, but restrict columns to samples from Mali
mali_genotypes <- query_by_metadata(con = con,
                                    chrom = "Chr05",
                                    start = 75104537,
                                    end = 75106403,
                                    meta_col = "countryorigin",
                                    meta_value = "Mali")

# Safely close out the database connection
disconnect_local_db(con)
}

}
