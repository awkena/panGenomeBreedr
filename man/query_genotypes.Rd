% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sqlite_variant_discovery.R
\name{query_genotypes}
\alias{query_genotypes}
\title{Query genotypes for specific variant IDs (local)}
\usage{
query_genotypes(
  con,
  variant_ids,
  variant_id_col = "variant_id",
  variants_table = "variants",
  genotypes_table = "genotypes",
  meta_data = NULL
)
}
\arguments{
\item{con}{A DBI connection object to the local database generated by \code{connect_local_db()}.}

\item{variant_ids}{A character vector of unique variant identifiers to look up (e.g., \code{c("SNP_Chr05_75104557")}).}

\item{variant_id_col}{A character value specifying the primary key column name for variant IDs. 
Defaults to \code{"variant_id"}.}

\item{variants_table}{A character value specifying the name of the variants reference table view. 
Defaults to \code{"variants"}.}

\item{genotypes_table}{A character value specifying the name of the genotype call data table view. 
Defaults to \code{"genotypes"}.}

\item{meta_data}{A character vector of metadata columns (e.g., \code{"chrom"}, \code{"pos"}, \code{"ref"}) 
to include from the variants table. If \code{NULL}, returns all available schema fields.}
}
\value{
A data frame containing mapped metadata tracking columns alongside individual sample genotype 
  calls formatted in a structured variants-by-samples matrix.
}
\description{
Extracts complete genotype call records along with specified variant metadata properties 
for an explicit vector list of unique variant identifiers. Automatically resolves 
and unpacks internal array-packed list formats seamlessly into standard wide columns.
}
\examples{
\dontrun{
# Load the package
library(panGenomeBreedr)

# Connect to the database pipeline
con <- connect_local_db(folder_path = "~/Desktop/curated_sorghum_variant_resource")

# Extract all sample genotypes for an explicit list of target marker strings
target_markers <- c("INDEL_Chr05_75104541", "SNP_Chr05_75104557")
marker_matrix <- query_genotypes(con = con, 
                                 variant_ids = target_markers,
                                 meta_data = c("chrom", "pos", "ref", "alt", "variant_type"))

# Safely close out the database connection
disconnect_local_db(con)
}

}
