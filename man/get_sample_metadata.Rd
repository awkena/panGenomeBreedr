% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Offline_variant_discovery.R
\name{get_sample_metadata}
\alias{get_sample_metadata}
\title{Get samples metadata from the database (local)}
\usage{
get_sample_metadata(con, query_col = NULL, query_value = NULL)
}
\arguments{
\item{con}{A DBI connection object to the local database generated by \code{connect_local_db()}.}

\item{query_col}{Character. Optional metadata column name to filter by (e.g., \code{"countryorigin"}). 
If \code{NULL}, all database metadata records are returned.}

\item{query_value}{Character or Numeric. The explicit attribute value to match within \code{query_col}.}
}
\value{
A data frame containing the sample metadata records, strictly sorted by their 
  \code{array_index} field to ensure perfect structural alignment with downstream genotype matrix.
}
\description{
Get samples metadata from the database (local)
}
\examples{
\dontrun{
# Load the package
library(panGenomeBreedr)

# Define the path to the curated_sorghum_variant_resource folder
my_db_folder <- "~/Desktop/curated_sorghum_variant_resource"

# Establish the connection
con <- connect_local_db(folder_path = my_db_folder)

# Fetch metadata for all sorghum accessions originating from Ethiopia
get_sample_metadata_result <- get_sample_metadata(con = con,
                                   query_col = "countryorigin",
                                   query_value = "Ethiopia")

print(get_sample_metadata_result)

# Disconnect at the end of your session
disconnect_local_db(con)

}

}
